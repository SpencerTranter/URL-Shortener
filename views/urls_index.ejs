<!DOCTYPE html>
<html lang="en">
<body>
  <header>
      <% include ./partials/_header %>
  </header>
  <main>
    <div>
      <ul>
        <% for(curr in urls){ %>
          <li> <%= curr %> </li>
        <% }; %>
      </ul>
      <a href=<%= shortener%>>Click here to shorten your next link!</a>
    </div>
  </main>
</body>
</html>